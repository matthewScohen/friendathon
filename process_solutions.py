def process_solutins(old_path: str, new_path: str):
    """
    Process old NYT connections puzzles from old_path.json and save them to new_path.pickle as a dict.

    :param old_path: Path to json file with puzzle data
    :param new_path: Path to save pickled dict to
    :return: dict of puzzles in the form of data[puzzle][level] = dict["group"/"members"]
    """
    pass